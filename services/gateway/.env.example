# API Gateway Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production-make-it-long-and-random
JWT_EXPIRES_IN=24h

# Supabase Configuration
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Note: The service role key is different from the anon key.
# It has elevated permissions and should NEVER be exposed to the frontend.
# Find it in: Supabase Dashboard > Project Settings > API > service_role key